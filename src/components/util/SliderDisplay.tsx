import React from 'react';
import {View, Text, ScrollView} from 'react-native';

const SliderDisplay = () => {
    return <View>
        
        <ScrollView>
            
        </ScrollView>
    </View>
}

export default SliderDisplay;